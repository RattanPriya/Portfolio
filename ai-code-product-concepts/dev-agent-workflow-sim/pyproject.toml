[project]
name = "dev-agent-workflow-sim"
version = "0.1.0"
description = "Workflow simulator for agentic developer support"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
